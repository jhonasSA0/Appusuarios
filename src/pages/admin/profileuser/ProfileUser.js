import React, { useRef} from 'react';
import ProfileUserTemplate from '../../../templates/admin/profileuser/ProfileUserTemplate';

export default function ProfileUser() {
  return(
    <ProfileUserTemplate/>
  ); 
}